/* 
👉 Create an array of all prime numbers up until a given number!

If input = 5, we would expect the function to return [2, 3, 5]
If input = 20, we would expect the function to return [2, 3, 5, 7, 11, 13, 17, 19]
*/

//👉 Write the function your CodeWarriors will start with below here:
export function generatePrimes(input) {
    
}